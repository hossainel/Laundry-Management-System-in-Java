name: Java CI & Build Artifact

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Package application with Maven
      run: mvn -B clean package --file pom.xml

    - name: Upload executable JAR as artifact
      uses: actions/upload-artifact@v4
      with:
        name: Laundry-Management-System-v1.0
        path: target/*.jar
        retention-days: 14
